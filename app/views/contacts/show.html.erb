<div class="container">
    <div class="row">
        <div class="col-sm-8 col-sm-offset-2">
			<h2>問い合わせ詳細</h2>
			<h3>投稿日</h3>
			<%= @contact.created_at.strftime('%Y/%m/%d %H:%M')%>
			<h3>名前</h3>
			<%= @contact.user.name %>
	  		<h3>本文</h3>
	  		<p><%=@contact.coment %></p>
	  		<!-- <div class="pull-right">
	  		<%= link_to '戻る', :back %>
	  		</div> -->
		</div>
	</div>
</div>
