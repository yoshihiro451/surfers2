<div class="container">
	<div class="row">
		<h1>新規投稿</h1>
		<%= form_for @topic do |f| %>
			<h4>タイトル</h4>
			<%= f.text_field :title %>
			<h4>コメント</h4>
			<%= f.text_area :comment %>
			<%= f.submit "投稿" %>
		<% end %>
	</div>
</div>
