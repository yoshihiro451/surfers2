<!-- <div class="container">
	<div class="row">
		<div class="col-xs-4">
			<h1>新規投稿</h1>
			<%= form_for @topic do |f| %>
				<h4>タイトル</h4>
				<%= f.text_field :title %>
				<h4>コメント</h4>
				<%= f.text_area :comment, :size => "80*200" %>
				<h4>地域</h4>
				<%= f.select :area, options_for_select(Topic.areas),{} %>
				<%= f.submit "投稿" %>
			<% end %>
		</div>
	</div>
</div>
 -->