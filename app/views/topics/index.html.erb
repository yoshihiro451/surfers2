<div class="container">
	<div class="row">
		<div class="col-xs-8 col-xs-offset-2">
			<h2>スレッド一覧</h2>
			<% @topics.each do |topic| %>
				<p>タイトル</p>
				<span><%= topic.title %></span>
				<p>コメント</p>
				<span><%= topic.comment %></span>
			<% end %>
		</div>
	</div>
</div>

